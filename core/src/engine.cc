#include <core/engine.h>
#include <iostream>

namespace mine {
  void Engine::init() {
    std::cout << "initializing: " << agents_ << " agents" << std::endl;
    for (int ii = 0; ii < agents_; ++ii) {

    }
  }
} // namespace mine
